CREATE TABLE blog_comment (id INTEGER PRIMARY KEY AUTOINCREMENT, entry_id INTEGER NOT NULL, body TEXT NOT NULL);
CREATE TABLE blog_entry (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, body TEXT NOT NULL);
